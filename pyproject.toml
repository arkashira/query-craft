[project]
name = "query_craft"
version = "0.1.0"
description = "Simple alerting for failed scheduled runs"
authors = [{name = "Axentx"}]
requires-python = ">=3.9"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
